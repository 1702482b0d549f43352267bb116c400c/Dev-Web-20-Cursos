# Use root/example as user/password credentials
version: '3.1'

services:

  db:
    image: mysql
    container_name: mysql 
    environment:
        MYSQL_ROOT_PASSWORD: 12345
        MYSQL_DATABASE: estudos
    volumes: 
        - ./data:/var/lib/mysql/estudos
    ports: 
        - '3306:3306'